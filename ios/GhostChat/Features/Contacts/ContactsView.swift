import SwiftUI

struct ContactsView: View {
    var body: some View {
        Text("Contacts — implemented in Stage 15")
            .foregroundStyle(.white)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color.black.ignoresSafeArea())
    }
}
